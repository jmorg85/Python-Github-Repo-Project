name: Test workflow

on:
    push:
        branches:
            - main
            
jobs:
    test:
        name: Run Python version 3.13
        runs-on: ubuntu-latest
        steps:
            - uses: actions/setup-python@v5
              with:
                python-version: '3.13'
            - run: python --version